

<%time = Time.new.in_time_zone("Warsaw").strftime("%Y-%m-%d %H:%M:%S") %>
<%if time.to_time.to_i>Classescalendar.find_by(group_id: @student.group_id , classes_number: 1).start.to_time.to_i && time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number: 1).end.to_time.to_i %>
    <%=link_to "Lekcja 1", lesson_path(:level => @progr.lvl), method: :post%><br></br>
<%else%>
    <%if time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number:1).start.to_time.to_i%>
        zajecia1!! dostepne od <%=Classescalendar.find_by(group_id: @student.group_id, classes_number: 1).start.strftime("%Y-%m-%d %H:%M:%S")%><br></br>
    <%else%>
        zajecia1!! Etap zakończony!!<br></br>
    <%end%>
<%end%>

<%if time.to_time.to_i>Classescalendar.find_by(group_id: @student.group_id, classes_number: 2).start.to_time.to_i && time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number: 2).end.to_time.to_i %>
    <%=link_to "Lekcja 2", lesson_path(:level => @progr.lvl), method: :post%><br></br>
<%else%>
    <%if time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number:2).start.to_time.to_i%>
        zajecia2!! dostepne od <%=Classescalendar.find_by(group_id: @student.group_id, classes_number: 2).start.strftime("%Y-%m-%d %H:%M:%S")%><br></br>
    <%else%>
        zajecia2!! Etap zakończony!!<br></br>
    <%end%>
<%end%>

<%if time.to_time.to_i>Classescalendar.find_by(group_id: @student.group_id, classes_number: 3).start.to_time.to_i && time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number: 3).end.to_time.to_i %>
    <%=link_to "Lekcja 3", lesson_path(:level => @progr.lvl), method: :post%><br></br>
<%else%>
    <%if time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number:3).start.to_time.to_i%>    
        zajecia3!! dostepne od <%=Classescalendar.find_by(group_id: @student.group_id, classes_number: 3).start.strftime("%Y-%m-%d %H:%M:%S")%><br></br>
    <%else%>
        zajecia3!! Etap zakończony!!<br></br>
    <%end%>    
<%end%>
<%if time.to_time.to_i>Classescalendar.find_by(group_id: @student.group_id, classes_number: 4).start.to_time.to_i && time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number: 4).end.to_time.to_i %>
    <%=link_to "Lekcja 4", lesson_path(:level => @progr.lvl), method: :post%><br></br>
<%else%>
    <%if time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number:4).start.to_time.to_i%>
        zajecia4!! dostepne od <%=Classescalendar.find_by(group_id: @student.group_id, classes_number: 4).start.strftime("%Y-%m-%d %H:%M:%S")%><br></br>
    <%else%>
        zajecia4!! Etap zakończony!!<br></br>
    <%end%>
<%end%>

<%if time.to_time.to_i>Classescalendar.find_by(group_id: @student.group_id, classes_number: 5).start.to_time.to_i && time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number: 5).end.to_time.to_i %>
    <%=link_to "Lekcja 5", lesson_path(:level => @progr.lvl), method: :post%><br></br>
<%else%>
    <%if time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number:5).start.to_time.to_i%>    
        zajecia5!! dostepne od <%=Classescalendar.find_by(group_id: @student.group_id, classes_number: 5).start.strftime("%Y-%m-%d %H:%M:%S")%><br></br>
    <%else%>
        zajecia5!! Etap zakończony!!<br></br>
    <%end%>
<%end%>

<%if time.to_time.to_i>Classescalendar.find_by(group_id: @student.group_id, classes_number: 6).start.to_time.to_i && time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number: 6).end.to_time.to_i %>
    <%=link_to "Lekcja 6", lesson_path(:level => @progr.lvl), method: :post%><br></br>
<%else%>
    <%if time.to_time.to_i<Classescalendar.find_by(group_id: @student.group_id, classes_number:6).start.to_time.to_i%>    
        zajecia6!! dostepne od <%=Classescalendar.find_by(group_id: @student.group_id, classes_number: 6).start.strftime("%Y-%m-%d %H:%M:%S")%><br></br>
    <%else%>
        zajecia6!! Etap zakończony!!<br></br>
    <%end%>
<%end%>
<br></br><br></br>
</div>
